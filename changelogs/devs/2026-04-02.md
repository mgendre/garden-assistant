---
date: 2026-04-02
title: "E20 — Variétés, associations courge musquée, redesign mes plantes"
---

### Backend

- **Entity** : ajout de `ParentPlantId` (Guid?, self-referencing FK) sur l'entité `Plant` avec migration EF Core. Relation `Parent` / `Varieties` configurée en Fluent API (US-306)
- **PlantSeeder** : seeding en deux passes — les espèces parentes d'abord, puis les variétés via le champ `parentKey` dans les fichiers JSON (US-307)
- **Seed data** : 27+ variétés ajoutées sur 17 espèces parentes (tomate, courge, haricot, basilic, pois, laitue, fraisier, framboisier, poivron, piment, chou, radis, fenouil, concombre, tournesol). Restructuration complète du haricot : fusion haricot-nain/haricot-à-rames en un parent unique avec 10 variétés (filet nain/rames, coco nain/rames, flageolet, à écosser). Ajout du haricot d'Espagne (P. coccineus) et de la courge musquée comme nouvelles espèces (US-308, US-308a, US-308b)
- **Associations courge musquée** : copie des 10 associations de `courge` vers `courge-musquee` dans `associations.json` pour que C. moschata hérite des mêmes compagnonnages que C. pepo (US-308)
- **Framboisier** : renommage de `framboisier-remontant` en `framboisier-d-automne` (clé et nom affiché)
- **Calendriers variété** : actions spécifiques par variété (tomate-naine sans taille, framboisier-d-automne, radis-d-hiver, courge-musquee, haricot-à-écosser). Héritage des actions du parent quand la variété n'en définit pas (US-308)
- **PlantService** : héritage des propriétés (`WaterNeeds`, `SunRequirement`, `SoilType`, `RootDepth`, `GrowthHabit`, `Lifecycle`, famille, genre) et des mécanismes (`PlantMechanisms`) du parent vers la variété. Héritage de `HarvestReadiness` et des actions calendrier (US-309)
- **PlantAssociationService** : résolution transparente des variétés vers leur parent pour le scoring d'associations. Les DTOs retournent les IDs de variétés d'origine mais affichent l'espèce parente dans les détails (US-310)
- **Validation seed data** : tests xUnit vérifiant les enums (`SunRequirement`, `SoilType`, etc.) dans tous les fichiers JSON de seed. Correction de valeurs invalides détectées (US-308)

### Frontend

- **NSwag** : client TypeScript régénéré avec les champs `parentPlantId`, `parentPlantKey`, `isVariety` sur `PlantDto` (US-311)
- **My-plants redesign** : remplacement des `<app-plant-card>` dépliables par une liste compacte `.plant-item` (emoji + nom + badge famille). Ajout d'un `<app-search-input>` pour filtrer par nom, nom latin ou famille via un signal `searchQuery` et un `computed` `filteredPlants`. Clic sur un item ouvre `PlantDialogService.openDetail()`, bouton supprimer via `MyPlantsStore.toggle()`. Le composant injecte désormais `CompanionStore` (pour `getPlantEmoji` / `getFamilyClass`) en plus de `MyPlantsStore`
- **Plant detail dialog** : correction du double padding, du padding-top excessif, et suppression de la bordure/ombre sur les plant-cards à l'intérieur du dialog (`.plant-detail-dialog .plant-card { box-shadow: none; border: none }`)
- **Association panels** : suppression de la bordure/ombre sur les plant-cards à l'intérieur des panels d'association pour éviter la double bordure

### Infrastructure

- Aucun changement d'infrastructure dans cette branche

### Points d'attention

- L'héritage de propriétés se fait au niveau service (pas en BDD) : une variété sans valeur propre reçoit celle de son parent au runtime
- Les associations d'une variété sont résolues via le `ParentPlantId` — aucune association n'est stockée directement sur les variétés
- Le seeding deux passes impose que les fichiers JSON de variétés réfèrent un `parentKey` correspondant au `Key` d'une espèce existante
- Les tests de validation des seed data (`SeedDataValidationTests`) sont à maintenir lors de l'ajout de nouvelles plantes ou variétés
- Le backlog E20 n'est pas encore marqué comme terminé — à mettre à jour après merge
